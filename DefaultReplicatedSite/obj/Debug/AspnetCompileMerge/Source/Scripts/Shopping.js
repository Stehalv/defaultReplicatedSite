var settings = {
    urls = {
        AddItemToCart = "@"
    }
}

$(".add-item-to-order").on("click", function () {
    var $form = $(this).closest('form').serialize();
    $.ajax({
        Url: 
    })
});